import { Stack } from '@chakra-ui/react'
import React from 'react'

const SideBar = () => {
  return (
    <Stack w={'20%'} position={'fixed'} height={'100vh'} backgroundColor={'yellow'} >
      {/* Add sidebar content here */}
    </Stack>
  )
}

export default SideBar;
